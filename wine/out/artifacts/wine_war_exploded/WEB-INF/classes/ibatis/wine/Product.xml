<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.chimu.wine.dao.ProductDao" >
    <resultMap id="BaseResultMap" type="com.chimu.wine.bean.ProductBean">
    </resultMap>

    <select id="getProductList" resultMap="BaseResultMap">
        SELECT * FROM product
    </select>

    <select id="getProductWithId" resultMap="BaseResultMap">
        SELECT * FROM product WHERE id = #{id}
    </select>

    <update id="modifyProduct">
        UPDATE product SET title = #{title}, submessage = #{submessage}, price = #{price}, volume = #{volume}, description = #{description}, origprice = #{origprice}, count = #{count}, freight_money = #{freight_money}, point = #{point}, sales = #{sales} WHERE id = #{id}
    </update>

</mapper>


