<?xml version="1.0" encoding="UTF-8"?>
<!-- 
    Welcome to use the TableGo Tools.
    
    http://vipbooks.iteye.com
    http://blog.csdn.net/vipbooks
    http://www.cnblogs.com/vipbooks
    
    Author:bianj
    Email:edinsker@163.com
    Version:5.0.0
-->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- MAIN_CASH -->
<mapper namespace="perfect_note.mainCash">
    <!-- 结果映射 -->
    <resultMap id="mainCashMap" type="MainCashEntity"></resultMap>
    
    <!-- 表所有字段 -->
    <sql id="allColumns">
        ID, UID, MONEY, DATE
    </sql>
    
    <!-- 根据条件查询数据 -->
    <select id="findMainCashByCondition" resultMap="mainCashMap" parameterType="map">
        SELECT
        <include refid="allColumns" />
        FROM MAIN_CASH WHERE 1 = 1
        <if test="uid != null and uid != ''">
            AND UID LIKE CONCAT('%',#{uid},'%')
        </if>
        <if test="money != null and money != ''">
            AND MONEY LIKE CONCAT('%',#{money},'%')
        </if>
    </select>
    
    <!-- 插入数据 -->
    <insert id="insertMainCash" parameterType="map">
        INSERT INTO MAIN_CASH (<include refid="allColumns" />)
        VALUES (
            #{id},
            #{uid},
            #{money},
            #{date}
        )
    </insert>
    
    <!-- 修改数据 -->
    <update id="updateMainCash" parameterType="map">
        UPDATE MAIN_CASH SET
            UID = #{uid},
            MONEY = #{money},
            DATE = #{date}
        WHERE ID = #{id}
    </update>
    
    <!-- 根据主键批量删除数据 -->
    <delete id="deleteMainCashByIds" parameterType="list">
        DELETE FROM MAIN_CASH WHERE ID IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>